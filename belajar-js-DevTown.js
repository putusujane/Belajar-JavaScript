// Mengambil input user
var name = prompt("What is your name?");

// Menampilkan notifikasi data di browser
window.alert("Hi " + name + ", Welcome to DevTown.");

// Menampilkan data di terminal atau console
console.log("Hello, World!");

// Declaring Function (Fungsi)
function hello(age) {
    var x = prompt("Ketik halo!");
    alert("Kamu mengetik " + x);
    return age / 2;
}
hello(27);

